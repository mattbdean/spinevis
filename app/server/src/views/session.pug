extends layout.pug

block append stylesheets
    link(rel='stylesheet', href='/style/base.min.css')

block content
    //- Pass our pug variable from the server into the session-vis attribute
    //- which gets bound to its controller
    session-vis(session-id=id)
